default:
  suites:
    default:
      paths: [ %paths.base%/features ]
      contexts: [ FeatureContext ]
  gherkin:
    filters:
  formatters:
    pretty: true
  extensions:
    Behat\MinkExtension:
      base_url: 'http://localhost/'
      selenium2:
        wd_host: "http://localhost:4444/"